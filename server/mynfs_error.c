#include "mynfs_error.h"

char *get_error_message(int err_no) {
/*
  char *message;

  switch(err_no) {
    case 1:
      strcpy(message, "smth");
      break;
    .
    .
    .
    default:
      break;
  }

  return message;	*/
}
